<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.ase.roulettegame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RouletteGame">
        <activity android:name=".Add5RouletteActivity"></activity>
        <activity android:name=".Add4RouletteActivity" />
        <activity android:name=".Add3RouletteActivity" />
        <activity android:name=".Add5Activity" />
        <activity android:name=".Add4Activity" />
        <activity android:name=".Add3Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>